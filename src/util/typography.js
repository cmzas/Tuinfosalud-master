import Typography from 'typography'

const typography = new Typography({
  baseFontSize: '18px',
  baseLineHeight: 1.8,
  headerFontFamily: ['Noto Sans', 'Roboto'],
  bodyFontFamily: ['Roboto', 'Noto Sans'],
})

export default typography
